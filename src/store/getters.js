const getters = {
    userInfo: state => state.userInfo,
    crumbs: state => state.crumbs,
    socketData: state => state.socketData,
    logList: state => state.logList,
    noLoginList: state => state.noLoginList,
    menus: state => state.menus,
    store: state => state.store,
    updateNumber: state => state.updateNumber
};

export default getters
